'use strict';

describe('Directive: ngGalleria', function () {

  // load the directive's module
  beforeEach(module('waijuyiApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});